#1. reversible MersenneTwisterPy3k
  prevInt(int), prevLong(long), prevBytes(byte[])

#2. generic reversible random generator tests

#3. implement the prevGaussian method

#4. ReversibleMersenneTwister - find a way to get rid of the initial state.
    try saving only the first and the last element from the state.
